# Insurance Broker AI — Dependencies
chainlit>=2.10.0
anthropic>=0.40.0
python-dotenv>=1.0.0

# MCP Server
fastmcp>=2.14.5
mcp>=1.25.0
pydantic>=2.12.0
pydantic-settings>=2.0.0
jinja2>=3.1.0
aiosqlite>=0.21.0

# Admin panel (FastAPI)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# Export: PDF / XLSX / DOCX
openpyxl>=3.1.0
python-docx>=1.0.0
weasyprint>=62.0

# Browser automation (Playwright — runs headless on Cloud Run)
playwright>=1.44.0

# Google Drive integration (Phase 2 — optional, gracefully skipped if not installed)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0

# Firebase Firestore — persistent storage across Cloud Run deploys
# Conversations, users, and project data survive container restarts
google-cloud-firestore>=2.16.0

# RAG / Knowledge Base
chromadb>=1.0.0                    # Vector store (persistent, file-based)
                                   # Embeddings: all-MiniLM-L6-v2 (local, no API key)
apscheduler>=3.10.0

# PDF generation + merging (insurance SAZ forms)
fpdf2>=2.8.0
pypdf>=5.0.0
Pillow>=10.0.0
google-cloud-storage>=2.10.0
