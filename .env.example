# Alex Insurance Broker — Environment Variables Template
# Copy to .env and fill in real values. NEVER commit .env to git.

# ── AI Providers ───────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...       # Claude Vision + chat (required)
GEMINI_API_KEY=AIza...              # Google Gemini fallback (optional)

# ── Email (SMTP — outbound from Alex) ───────────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=fungadgetsgames@gmail.com
SMTP_PASS=<gmail-app-password>     # 16-char Gmail App Password
SMTP_FROM_NAME=Alex Broker

# ── Email (IMAP — inbound broker emails) ────────────────────────
IMAP_HOST=mail.ze-one.de
IMAP_PORT=993
IMAP_USER=AlexAi@tpsh.ro
IMAP_PASS=<imap-password>

# ── Google Cloud ─────────────────────────────────────────────────
FIRESTORE_PROJECT=project-79fa7e7d-5de7-4c32-b09
GCS_BUCKET=alex-broker-reports

# ── TPSH Branding (PDF footer) ───────────────────────────────────
TPSH_ADDRESS=Musterstraße 1, 10115 Berlin, Deutschland
TPSH_PHONE=+49 30 0000-0000
TPSH_EMAIL=info@tpsh.de
TPSH_WEBSITE=www.tpsh.de

# ── Runtime ──────────────────────────────────────────────────────
PORT=8080
LOG_LEVEL=INFO
